\documentclass[12pt]{article}
\usepackage{sbc-template}
\usepackage{amsmath,amssymb}
\usepackage{graphicx,url,booktabs}
\makeatletter\g@addto@macro\UrlBreaks{\do\-\do\/\do\.\do\_}\makeatother
\Urlmuskip=0mu plus 1mu\relax
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{enumitem}
\usepackage{pifont}
\usepackage{array}
\usepackage{xcolor}
\usepackage{breakcites}
\graphicspath{{figures/}}
\setlength{\textfloatsep}{5pt plus 2pt minus 2pt}
\setlength{\intextsep}{5pt plus 2pt minus 2pt}
\setlength{\floatsep}{5pt plus 2pt minus 2pt}
\emergencystretch=3em  % absorve overfull de listas \texttt{} (nomes de repo)
% Float placement (match the companion paper): keep figures/tables near their text.
\usepackage{float}
\renewcommand{\topfraction}{0.95}
\renewcommand{\bottomfraction}{0.95}
\renewcommand{\textfraction}{0.05}
\renewcommand{\floatpagefraction}{0.95}
\newcommand{\cmark}{\textcolor{green!60!black}{\ding{51}}}
\newcommand{\xmark}{\textcolor{red!70!black}{\ding{55}}}
\usepackage[hidelinks,colorlinks=true,allcolors=black]{hyperref}
\raggedbottom  % folga vai pro rodape, evita vaos esticados em paginas curtas
% footnotes menores
\makeatletter
\renewcommand\@makefntext[1]{\parindent 1em\noindent\hb@xt@1.8em{\hss\@makefnmark}\scriptsize #1}
\makeatother

% patch p/ o bug \bbl@normalsf (sbc-template + babel em TeXLive recente)
\makeatletter
\gdef\normalsfcodes{}
\gdef\bbl@normalsf{}
\AddToHook{begindocument/before}[patch.normalsfcodes]{%
  \gdef\normalsfcodes{}%
  \gdef\bbl@normalsf{}%
}
\DeclareHookRule{begindocument/before}{patch.normalsfcodes}{before}{babel}
\AddToHook{shipout/before}{\gdef\normalsfcodes{}\gdef\bbl@normalsf{}}
\makeatother

% ============================================================
% RASCUNHO: artigo CURTO (SBSeg, ~6 paginas). Numeros marcados
% PROVISORIO sao do scan parcial (atualizar ao fechar o censo).
% Intro/Relacionados: preencher apos ler os PDFs completos (sem plagio).
% ============================================================

\title{How Secure Is Your Base? A Recent Multi-Scanner\\
Census of Linux Operating-System Images on Docker Hub}

\author{Anonymous}
\address{Anonymous Institution}

\begin{document}
\maketitle

\begin{abstract}
% PROVISORIO: fechar com numeros finais do censo
Every container starts from an operating-system (OS) base image, yet these are
usually measured with a single scanner. We present a recent multi-scanner
census of Docker Hub's Linux OS base images: \textbf{5{,}606} unique
\texttt{amd64} images across \textbf{20} repositories, scanned with \textbf{14}
open-source tools. We find that vulnerability posture varies by an order of
magnitude across distributions and is driven by image age and a distribution's
patch cadence rather than by image size or popularity; and that about one in
five historically-published images can no longer be pulled by a modern Docker
(legacy manifest schema). As a secondary result, the four package-vulnerability
engines barely agree, so the posture a single tool reports is largely a property
of that tool. We publicly release this multi-scanner dataset, to our knowledge
the first focused on OS base images, together with the full pipeline.
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:intro}
Containers are now a default unit of software delivery: Docker reports more
than 20~million developers\footnote{\href{https://www.docker.com/press-release/unveils-2024-state-of-application-development-report/}{Docker, 2024 report}}
and, in its 2025 survey, 92\% of IT professionals using
containers\footnote{\href{https://www.docker.com/blog/2025-docker-state-of-app-dev/}{Docker, 2025 survey}}.
Underneath them sits Docker Hub, the largest public registry, with on the order
of 8.3~million image repositories and hundreds of billions of cumulative
pulls\footnote{\href{https://www.docker.com/blog/docker-index-shows-continued-massive-developer-adoption-and-activity-to-build-and-share-apps-with-docker/}{Docker Index}}. Every one of those images ultimately begins with
a single line, \texttt{FROM} an operating-system (OS) base image, and a
handful of bases are the common root of nearly all of them: \texttt{alpine},
\texttt{ubuntu}, \texttt{debian} and \texttt{busybox} each record more than a
billion pulls, and Debian, Alpine and Ubuntu dominate the base-image choice of
the ecosystem\footnote{\href{https://anchore.com/blog/breakdown-of-operating-systems-of-dockerhub/}{Anchore, 2024}}. Because a flaw in a base image is
inherited, unchanged, by every image built on it~\cite{shu2017dockerhub}, the
security posture of these few OS bases is a property the whole ecosystem
inherits.

That inherited posture is poor and consequential. Base images are the root of
the software supply chain, so their known vulnerabilities are a supply-chain
risk in their own right, alongside the malicious-injection attacks that dominate
headlines (projected to cost USD~60~billion in
2025\footnote{\href{https://cybersecurityventures.com/software-supply-chain-attacks-to-cost-the-world-60-billion-by-2025/}{Cybersecurity Ventures}} and predicted by Gartner to hit
45\% of organisations by 2025\footnote{\href{https://www.gartner.com/en/newsroom/press-releases/2022-03-07-gartner-identifies-top-security-and-risk-management-trends-for-2022}{Gartner, 2022}}). And the inherited
risk is large: popular Docker Hub images carry hundreds of known vulnerabilities
on average, many of them years old\footnote{\href{https://www.netrise.io/resources-whitepaper-brief/supply-chain-visibility-risk-study-edition-2}{NetRise, 2024}}, against a backdrop of
record CVE disclosure (over 48{,}000 CVEs in 2025)\footnote{\href{https://www.cvedetails.com/browse-by-date.php}{CVE Details}}. Worse, some of
the most-pulled OS bases are no longer maintained (every official
\texttt{centos} tag reached end-of-life in June~2024) yet remain in heavy
use.

Despite this central role, the security posture of OS base images has usually
been characterised with a \emph{single} scanner and on samples that age
quickly: the large-scale Docker Hub measurements have typically used one
detector~\cite{shu2017dockerhub,zerouali2019outdated,liu2020understanding,shi2025drdocker},
and the studies that \emph{do} compare scanners have done so on focused
samples~\cite{mills2023longitudinal,boles2024deciphering}. No recent work measures
the Linux OS base images specifically, with a heterogeneous multi-scanner
battery, as a current census. This paper fills that gap.

\noindent\textbf{Contributions.} We present (i)~a recent multi-scanner census of
Docker Hub's Linux OS base images (Section~\ref{sec:method}); (ii)~a
per-distribution posture, a staleness-vulnerability relationship, and the
finding that vulnerability load is driven by age rather than image size or
popularity (Sections~\ref{sec:rq1},~\ref{sec:rq2},~\ref{sec:rq5});
(iii)~an image-longevity finding (Section~\ref{sec:rq4}); (iv)~as a secondary
result, a four-engine divergence measurement on the OS-package layer
(Section~\ref{sec:rq3}); and (v)~\textbf{a public multi-scanner dataset}, to our
knowledge the first focused on OS base images: the per-image findings of all 14
scanners over the census, released with the full pipeline so the corpus can be
re-analysed under other metrics or scanners without repeating the costly scan.

% ============================================================
\section{Related Work}
\label{sec:related}
Table~\ref{tab:related} positions our work against prior measurements along six axes.

\begin{table}[!htbp]
\centering
\caption{Prior OS/base-image and container-scanner measurements.}
\label{tab:related}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{l r c c l c c}
\toprule
\textbf{Study} & \textbf{Images} & \textbf{Tools} & \textbf{Dims} & \textbf{Source} & \textbf{OS} & \textbf{X-sc.} \\
\midrule
\cite{shu2017dockerhub} & 356{,}218 & 1 & V & Hub crawl & \xmark & \xmark \\
\cite{zerouali2019outdated} & 7{,}380   & 1$^{\ddagger}$ & V & Hub, Debian & \cmark & \xmark \\
\cite{liu2020understanding} & 2.2\,M$^{\ast}$ & 1 & V & Hub crawl & \xmark & \xmark \\
\cite{ibrahim2020toomany} & 936       & 1 & V & Hub, per-sys & \xmark & \xmark \\
\cite{zerouali2021techlag} & 140{,}498 & 1$^{\ddagger}$ & V & Hub, Debian & \cmark & \xmark \\
\cite{haque2022wellbegun} & 261       & 1 & V & Hub, official & \cmark & \xmark \\
\cite{dahlmanns2023secrets} & 337{,}171 & 1 & S & Hub crawl & \xmark & \xmark \\
\cite{mills2023longitudinal} & 380       & 6 & V & Hub categories & \xmark & \cmark \\
\cite{boles2024deciphering} & 927       & 2 & V & Hub official & \cmark & \cmark \\
\cite{bufalino2025sbomproof} & 20$^{\dagger}$ & 7 & V & Hub Deb/Alp & \cmark & \cmark \\
\cite{shi2025drdocker} & 33{,}952$^{\ast}$ & 1 & V,S,C,M & Hub pull-rank & \xmark & \xmark \\
\midrule
\textbf{This work} & \textbf{5{,}606} & \textbf{14} & \textbf{V,B,S,C,M} & \textbf{Hub category} & \cmark & \cmark \\
\bottomrule
\end{tabular}}\\[3pt]
{\footnotesize Dims: V~vuln, B~SBOM, S~secrets, C~config, M~malware. X-sc.: cross-scanner.
$\ast$~vulnerability scan on a subset; $\dagger$~top-20 Debian/Alpine image families;
$\ddagger$~Debian Security Tracker (metadata), not a scanner.}
\end{table}

\noindent\textbf{Large-scale measurements.} A rich line of work has charted the security of Docker Hub. \cite{shu2017dockerhub}
introduced the first large-scale study, scanning 356{,}218 images and showing
that vulnerabilities propagate from a base image to everything built on it.
\cite{liu2020understanding} extended the scale to 2.2~million images;
\cite{zerouali2019outdated} introduced the notion of \emph{technical lag} on
7{,}380 Debian-based images, later generalised to a multi-dimensional analysis
over 140{,}000 images~\cite{zerouali2021techlag}. \cite{wist2021vulnerability}
analysed 2{,}500 images across image classes, \cite{ibrahim2020toomany} examined
how images for the same system differ across base distributions, and the recent
\cite{shi2025drdocker} ranked influential images by pull count at the scale of
the full registry. Most relevant to us, \cite{haque2022wellbegun} characterised
the exploitability and impact of vulnerabilities in 261 official base images and
reported a result we revisit in Section~\ref{sec:rq5}: highly-exploitable
vulnerabilities are \emph{more} frequent in minimal images such as Alpine, while
larger distributions carry the higher-impact ones. These studies established the
prevalence and propagation of vulnerabilities in the ecosystem; each draws on a
single vulnerability detector, and our multi-scanner design complements them by
also quantifying how much the measured posture depends on the chosen scanner.

\noindent\textbf{Inter-scanner divergence.} Prior scanner comparisons on focused
samples report large disagreement~\cite{boles2024deciphering,bufalino2025sbomproof};
we revisit this as a secondary result, at OS-base census scale
(Section~\ref{sec:rq3}).

\noindent\textbf{Gap.} Building on this body of work, we are not aware of a recent study that measures
the Linux OS base images of Docker Hub specifically with a heterogeneous
multi-scanner battery as a current census, nor one that quantifies the
prevalence and popularity of \emph{end-of-life} OS images that remain in heavy
use. This paper addresses both.

% ============================================================
\section{Methodology}
\label{sec:method}
% Esta secao e NOSSA, escrita original.

\noindent\textbf{Corpus.} We take as our corpus the operating-system base images
Docker Hub lists under its \emph{Operating systems} category: \textbf{20}
repositories with an \texttt{amd64} image (\texttt{alpine}, \texttt{ubuntu},
\texttt{debian}, \texttt{centos}, \texttt{busybox}, \texttt{amazonlinux},
\texttt{fedora}, \texttt{oraclelinux}, \texttt{rockylinux}, \texttt{almalinux},
\texttt{photon}, \texttt{archlinux}, \texttt{tumbleweed}, \texttt{leap},
\texttt{kali-rolling}, \texttt{mageia}, \texttt{alt}, \texttt{cirros},
\texttt{clearlinux}, \texttt{sl}).
Deduplicating tags by \texttt{amd64} content digest (e.g.\ \texttt{24.04},
\texttt{noble} and \texttt{latest} are one image) leaves \textbf{5{,}606} unique
images, the unit of measurement.

\noindent\textbf{Scanner battery.} An OS base image is a static root filesystem with no application source code
and no running service, so we select the static-analysis axes that apply to
such an artifact and run \emph{14} open-source
scanners.\footnote{Each scanner runs from a version-pinned container image; the
exact versions are recorded in the released configuration.} \emph{SBOM}: Syft and
cdxgen. \emph{Package vulnerabilities (SCA)}: Trivy, Grype, OSV-Scanner and
Clair. \emph{Image hardening}: Dockle and Checkov. \emph{Secrets}:
TruffleHog, Gitleaks, detect-secrets and Whispers. \emph{Malware/IOC}:
ClamAV and YARA\-Hunter. We exclude the SAST, dynamic (DAST) and network axes,
which need application source or a running service a base image lacks. Each
image is exported once to a tar archive that all 14 scanners read, so result
differences reflect the scanner rather than the pull, and raw outputs are kept
verbatim.

% ============================================================
\section{Results}
\label{sec:results}

\subsection{RQ1: Security posture by distribution}
\label{sec:rq1}
\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_rq1.pdf}
\caption{(a)~mean critical+high per distribution; (b)~severity mix;
(c)~critical prevalence; (d)~mean total vulnerabilities.}
\label{fig:rq1}
\end{figure}
Critical- and high-severity vulnerability findings vary by an order of
magnitude across distributions (Figure~\ref{fig:rq1}(a)). Debian carries the
most (mean $\approx$344 critical+high per image, $n=270$), followed by the RHEL
family (CentOS $\approx$206, OracleLinux $\approx$189, AmazonLinux $\approx$152,
RockyLinux $\approx$115); Alpine sits lower ($\approx$84), and Arch, ALT, Kali,
Mageia and openSUSE~Tumbleweed report few or none. Within-distribution variance
is large (Debian's SD, $\approx$564, exceeds its mean) and some repositories are
small (CentOS $n=10$), so these are tendencies, not tight estimates. Package
count does not track this ordering: Mageia and Fedora carry many packages yet
few critical or high findings (Section~\ref{sec:rq5}).

\subsection{RQ2: Staleness vs.\ vulnerability}
\label{sec:rq2}
\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_rq2.pdf}
\caption{(a)~mean total by age ($\pm$1~SD); (b)~critical and high by age;
(c)~per-image age vs.\ total; (d)~critical prevalence by age.}
\label{fig:rq2}
\end{figure}
Vulnerability count tends to rise with image age (Figure~\ref{fig:rq2}(a)).
Images rebuilt within the last six months carry a mean of $\approx$255 total
vulnerability findings, climbing through $\approx$273 (one to two years) and
$\approx$457 (two to four years) to $\approx$692 for images not rebuilt in over
four years. The per-image rank correlation is positive but modest (Spearman
$\rho=0.16$, $p<0.001$), reflecting wide within-bucket variance even as the
bucketed means rise monotonically (the technical lag of
\cite{zerouali2019outdated}, here across 20 distributions).

\subsection{RQ3: Inter-scanner divergence on OS packages}
\label{sec:rq3}
\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_rq3.pdf}
\caption{(a)~pairwise CVE Jaccard; (b)~CVEs per engine; (c)~CVEs by number of
agreeing engines; (d)~engine-only CVEs.}
\label{fig:rq3}
\end{figure}
The four package-vulnerability engines barely agree (Figure~\ref{fig:rq3}).
We compare at the \mbox{(image, CVE)} level because the engines name packages
differently (Clair reports source packages, Trivy and Grype binary packages), so
a package-level key would understate agreement. Even so, the best-agreeing pair
is Trivy and Grype, at a Jaccard of just \textbf{0.35}; every other pair is below
0.05, and OSV-Scanner and Clair are near-disjoint from the rest
(OSV$\cap$Clair $=0$). The engines differ sharply in scope: Grype and Trivy
report by far the most image-CVE pairs (196k and 148k), OSV-Scanner targets
language-ecosystem advisories (9k) and Clair maps only specific distributions to
its feeds (8k, and zero on Rocky and AlmaLinux, which it does not map to the RHEL
feeds). The reported security posture of an OS image is thus, to a large degree,
a property of the chosen scanner.

\subsection{RQ4: End-of-life and un-pullable images}
\label{sec:rq4}
Two longevity hazards surface. First, about \textbf{one in five} of the
digest-pinned OS images fail to pull on a current Docker Engine
with \texttt{manifest schema unsupported}: they are old images in the deprecated
Docker image-manifest schema~1, whose support modern Docker removed. The digests
still exist on Docker Hub, but the images are no longer installable, and the rate
varies sharply by distribution (Figure~\ref{fig:rq4}): over half of the
\texttt{centos} and \texttt{busybox} digests and about a third of \texttt{ubuntu}
are affected, against under 3\% for \texttt{debian}, \texttt{archlinux} and
\texttt{rockylinux}. Second, end-of-life distributions remain in heavy use:
every \texttt{centos} tag has been unmaintained since June~2024, yet the
repository still records over a billion pulls. To our knowledge neither the
prevalence of legacy-schema OS images nor the popularity of EOL bases has been
measured before.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_rq4.pdf}
\caption{(a)~un-pullable rate by distribution; (b)~repository pulls;
(c)~un-pullable count; (d)~rate vs.\ pulls.}
\label{fig:rq4}
\end{figure}

\subsection{RQ5: What drives the vulnerability load?}
\label{sec:rq5}
\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_rq5.pdf}
\caption{(a)~packages vs.\ total; (b)~standardized regression coefficients
(age vs.\ packages); (c)~popularity (pulls) vs.\ total; (d)~vulnerabilities per
package by distribution.}
\label{fig:rq5}
\end{figure}
The intuition that a smaller image is a safer one does not hold cleanly
(Figure~\ref{fig:rq5}(a)). The relationship between package count and
vulnerability count is not monotonic: BusyBox, with a single package, carries
only a handful of findings, but Mageia, with around 250 packages, carries about
a dozen, while Debian carries hundreds of critical and high findings with fewer
packages than Mageia. This refines \cite{haque2022wellbegun}:
on our multi-scanner data the vulnerability count is governed more by a
distribution's advisory coverage and patch cadence than by image size.

A multiple regression makes the ranking explicit (Figure~\ref{fig:rq5}(b)): with
both predictors standardized, image age dominates ($\beta\approx0.44$) over
package count ($\beta\approx0.17$), and the partial correlations agree
($0.44$ vs.\ $0.19$); the vulnerability load tracks patch cadence, not image
size. Popularity offers no protection either: a repository's pull count is
essentially uncorrelated with its vulnerability count (Spearman
$\rho\approx0$, Figure~\ref{fig:rq5}(c)), so the most-pulled bases are no safer
than the rest.

\subsection{Beyond vulnerabilities: secrets, misconfiguration, malware}
\label{sec:other}
The battery also covers four non-vulnerability axes.
\emph{Misconfiguration:}
Dockle and Checkov flag at least one CIS-style issue in essentially every image
(missing \texttt{HEALTHCHECK}, content-trust disabled, running as \texttt{root});
these are structural properties, so the near-universal rate is robust.
\emph{Secrets:} TruffleHog and Gitleaks raise at least one detection in
\textbf{72\%} of images, but these are raw hits; as \cite{dahlmanns2023secrets}
(8.5\% validated) and Dr.\ Docker~\cite{shi2025drdocker} (99.3\% of hits filtered
as invalid) show, the validated rate is far lower, and validation is future work.
\emph{Malware/IOC:} ClamAV and YARA-Hunter match on many images, but inspection
shows these are mostly signatures hitting benign system binaries, so we treat
them as triage candidates, not confirmed malware (Dr.\ Docker confirmed only 24
in 33{,}952). As in RQ3, raw single-tool counts overstate.

\subsection{Reproducing prior analyses}
\label{sec:repro}
We reproduce concrete analyses from prior work on \emph{our} OS-base corpus
(Table~\ref{tab:repro}); their own corpora range from the whole registry to a
single distribution (the \emph{OS?} column marks the OS-base-focused ones), yet
the direction holds in every case, and the multi-scanner setting refines the
minimal-image and scanner-agreement findings.

\begin{table}[!htbp]
\centering
\caption{Prior analyses reproduced on our OS-base corpus.}
\label{tab:repro}
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.2}
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}} p{0.17\columnwidth} p{0.26\columnwidth} c l l @{}}
\toprule
\textbf{Study} & \textbf{Metric reproduced} & \textbf{OS?} & \textbf{Reported} & \textbf{Ours} \\
\midrule
\cite{shu2017dockerhub} & images with a high-severity vuln & \xmark & $>$80\% & 97\%$^{\ast}$ \\
\cite{zerouali2019outdated} & vulnerabilities vs.\ image age & \cmark & rises with age & $\approx$255$\to$692 ($\rho{=}0.16$) \\
\cite{ibrahim2020toomany} & spread across base distributions & \xmark & varies & 10$\times$ range \\
\cite{wist2021vulnerability} & where severe vulns concentrate & \xmark & language ecosys.\ & OS-package layer \\
\cite{haque2022wellbegun} & is a minimal image safer? & \cmark & no & no (non-monotonic) \\
\cite{dahlmanns2023secrets} & images carrying secrets & \xmark & 8.5\% (valid.) & 72\% (raw) \\
\cite{mills2023longitudinal} & vulnerabilities vs.\ time; OS effect & \xmark & rise; Debian $\gg$ & rise; Debian top \\
\cite{boles2024deciphering} & scanner divergence on OS images & \cmark & agree 15\% & Jaccard 0.35 \\
\cite{shi2025drdocker} & known-vulnerability prevalence & \xmark & 93.7\% & 98.7\% \\
\bottomrule
\end{tabular*}\\[2pt]
{\footnotesize \emph{OS?}: prior study's corpus is OS base images.
$\ast$~critical or high (Shu reports high-severity); $\rho$: Spearman.}
\end{table}

% ============================================================
\section{Limitations and conclusion}
\label{sec:conclusion}
\textbf{Limitations.} Small repositories carry wide within-distribution
variance, so per-distribution means are tendencies, not tight estimates.
We report raw scanner output without adjudicating false positives or negatives;
the divergence of RQ3 implies no engine is ground truth, and severity rests on
CVSS scores that can overstate risk without VEX or reachability
data~\cite{zhou2026realitycheck}. We scan \texttt{linux/amd64} only, characterise
each image as of its scan time, and cover only the \emph{Operating systems}
category; the released dataset lets others re-run the analysis under other
choices.
\textbf{Conclusion.} We measured the security posture of Docker Hub's Linux OS
base images with 14 scanners rather than one. Posture varies by an order of
magnitude across distributions and is driven by image age rather than image size
or popularity; and about a fifth of historically-published images can no longer
be installed by a modern Docker while end-of-life bases stay heavily pulled.
Secondarily, the four package-vulnerability engines agree on almost nothing, so
a single tool's count is largely a property of that tool. We publicly release
the consolidated multi-scanner dataset and pipeline\footnote{Released on
acceptance.} so the measurement can be reproduced, audited, and extended.

\bibliographystyle{sbc}
% Aperto de espacamento das referencias -- DESATIVADO (ha folga de espaco; usa o padrao SBC).
% Reativar (envolvendo o \bibliography) se faltar espaco:
% \begingroup
% \makeatletter
% \let\oldthebibliography\thebibliography
% \let\endoldthebibliography\endthebibliography
% \renewcommand{\thebibliography}[1]{%
%   \oldthebibliography{#1}%
%   \setlength{\itemsep}{-0.5ex}%
%   \setlength{\parskip}{4pt}%
%   \setlength{\parsep}{0pt}%
%   \setlength{\topsep}{0pt}%
% }
% \makeatother
\bibliography{paper}
% \endgroup
\end{document}
